# План разработки: Рефакторинг приложения PokerCardRecognizer

## Обзор проекта

Приложение PokerCardRecognizer - это iOS-приложение для отслеживания игр в покер и бильярд. Текущая реализация использует SwiftUI для UI, CoreData для хранения данных и базовую аутентификацию.

## Текущая архитектура

### Модели данных (CoreData)
- **Game**: Основная сущность игры (timestamp, gameType, связи с игроками)
- **Player**: Сущность игрока (name, buyin)
- **GameWithPlayer**: Связующая таблица (game, player, buyin, cashout)
- **BilliardBatche**: Сущность для партий бильярда

### Компоненты
- **Persistence.swift**: Управление CoreData стеком
- **AuthViewModel**: Аутентификация (логин/пароль, Face ID)
- **MainView**: Основной интерфейс со статистикой
- **ContentView**: Корневой view с управлением аутентификацией

---

## Фаза 1: Расширение модели данных и логики пользователей

### 1.1 Создание системы пользователей

**Приоритет: Высокий**  
**Срок: 1-2 недели**

#### Задачи:
1. **Создать новую модель User в CoreData**
   ```swift
   - userId: UUID (Primary Key)
   - username: String
   - email: String?
   - passwordHash: String
   - createdAt: Date
   - lastLoginAt: Date
   - subscriptionStatus: String (enum: free, premium)
   - subscriptionExpiresAt: Date?
   ```

2. **Расширить модель Game**
   ```swift
   - creatorUserId: UUID (ссылка на User)
   - createdAt: Date
   ```

3. **Создать модель PlayerProfile**
   ```swift
   - profileId: UUID
   - userId: UUID? (nullable - для связи с зарегистрированным пользователем)
   - displayName: String
   - isAnonymous: Bool
   - createdAt: Date
   ```

4. **Создать модель PlayerAlias**
   ```swift
   - aliasId: UUID
   - profileId: UUID (ссылка на PlayerProfile)
   - aliasName: String (например, "Антон", "Anton", "Антоха")
   - claimedAt: Date?
   ```

5. **Обновить AuthViewModel**
   - Добавить регистрацию пользователей
   - Хранить currentUserId в UserDefaults
   - Реализовать хеширование паролей (использовать CryptoKit)
   - Добавить восстановление пароля (заготовка)

### 1.2 Логика разделения игр

**Приоритет: Высокий**  
**Срок: 1 неделя**

#### Задачи:
1. **Создать GameService**
   ```swift
   class GameService {
       func getGamesCreatedByUser(_ userId: UUID) -> [Game]
       func getGamesParticipatedByUser(_ userId: UUID) -> [Game]
       func getUserStatistics(_ userId: UUID) -> UserStatistics
   }
   ```

2. **Создать структуру UserStatistics**
   ```swift
   struct UserStatistics {
       let totalGamesCreated: Int
       let totalGamesParticipated: Int
       let totalBuyins: Decimal
       let totalCashouts: Decimal
       let currentBalance: Decimal // cashouts - buyins
       let winRate: Double
       let profitByGameType: [String: Decimal]
       let recentGames: [GameSummary]
   }
   ```

3. **Реализовать фильтрацию в MainView**
   - Добавить сегментированный контрол: "Мои игры" / "Участвовал"
   - Обновить @FetchRequest с предикатами

---

## Фаза 2: Функция присвоения анонимных игроков

### 2.1 UI для связывания профилей

**Приоритет: Средний**  
**Срок: 1-2 недели**

#### Задачи:
1. **Создать ClaimPlayerView**
   - Показать список всех анонимных имен (Player.name)
   - Поиск по имени
   - Группировка похожих имен (fuzzy matching)
   - Превью: сколько игр будет связано
   - Кнопка "Это я" для каждого имени

2. **Создать PlayerClaimService**
   ```swift
   class PlayerClaimService {
       func findUnclaimedPlayers() -> [String]
       func claimPlayer(name: String, forUser userId: UUID)
       func suggestSimilarNames(_ name: String) -> [String]
       func getPlayerStatistics(name: String) -> PlayerStats
   }
   ```

3. **Логика присвоения**
   - При клике на "Это я":
     - Создать или найти PlayerProfile для текущего пользователя
     - Создать PlayerAlias с именем игрока
     - Обновить все GameWithPlayer записи, где player.name == aliasName
     - Связать их с PlayerProfile

4. **Добавить подтверждение**
   - Alert с информацией о количестве игр
   - Предупреждение, что действие необнеобратимо
   - Показать статистику (баланс, количество игр)

### 2.2 История присвоений

**Приоритет: Низкий**  
**Срок: 3-5 дней**

#### Задачи:
1. Создать экран "Мои профили"
2. Показать список всех связанных имен
3. Возможность добавить новый псевдоним вручную

---

## Фаза 3: Миграция на PostgreSQL и FastAPI

### 3.1 Создание FastAPI проекта

**Приоритет: Высокий**  
**Срок: 2-3 недели**

#### Структура проекта:
```
poker-api/
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── config.py
│   ├── database.py
│   ├── models/
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── game.py
│   │   ├── player.py
│   │   └── game_player.py
│   ├── schemas/
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── game.py
│   │   └── statistics.py
│   ├── api/
│   │   ├── __init__.py
│   │   ├── v1/
│   │   │   ├── __init__.py
│   │   │   ├── auth.py
│   │   │   ├── games.py
│   │   │   ├── players.py
│   │   │   └── statistics.py
│   ├── services/
│   │   ├── __init__.py
│   │   ├── auth_service.py
│   │   ├── game_service.py
│   │   └── player_service.py
│   └── utils/
│       ├── __init__.py
│       ├── security.py
│       └── dependencies.py
├── alembic/
│   └── versions/
├── tests/
├── requirements.txt
├── .env.example
└── README.md
```

#### Задачи:

1. **Настройка окружения**
   - Python 3.11+
   - FastAPI + Uvicorn
   - SQLAlchemy 2.0
   - Alembic для миграций
   - psycopg2-binary
   - python-jose для JWT
   - passlib для хеширования
   - pydantic-settings

2. **Создание моделей SQLAlchemy**
   ```python
   # models/user.py
   class User(Base):
       __tablename__ = "users"
       id = Column(UUID, primary_key=True)
       username = Column(String, unique=True)
       email = Column(String, unique=True)
       password_hash = Column(String)
       subscription_status = Column(String)
       created_at = Column(DateTime)
   
   # models/game.py
   class Game(Base):
       __tablename__ = "games"
       id = Column(UUID, primary_key=True)
       creator_user_id = Column(UUID, ForeignKey("users.id"))
       game_type = Column(String)
       timestamp = Column(DateTime)
       
   # models/player_profile.py
   class PlayerProfile(Base):
       __tablename__ = "player_profiles"
       id = Column(UUID, primary_key=True)
       user_id = Column(UUID, ForeignKey("users.id"), nullable=True)
       display_name = Column(String)
       is_anonymous = Column(Boolean)
   
   # models/game_player.py
   class GamePlayer(Base):
       __tablename__ = "game_players"
       id = Column(UUID, primary_key=True)
       game_id = Column(UUID, ForeignKey("games.id"))
       player_profile_id = Column(UUID, ForeignKey("player_profiles.id"))
       buyin = Column(Numeric)
       cashout = Column(Numeric)
   ```

3. **Создание API endpoints**

   **Auth endpoints:**
   - POST /api/v1/auth/register
   - POST /api/v1/auth/login
   - POST /api/v1/auth/refresh
   - GET /api/v1/auth/me

   **Game endpoints:**
   - GET /api/v1/games (с фильтрами: created_by_me, participated)
   - POST /api/v1/games
   - GET /api/v1/games/{game_id}
   - PUT /api/v1/games/{game_id}
   - DELETE /api/v1/games/{game_id}

   **Player endpoints:**
   - GET /api/v1/players/unclaimed
   - POST /api/v1/players/claim
   - GET /api/v1/players/my-profiles
   - POST /api/v1/players/aliases

   **Statistics endpoints:**
   - GET /api/v1/statistics/me
   - GET /api/v1/statistics/balance
   - GET /api/v1/statistics/by-game-type

4. **Реализация JWT аутентификации**
   - Access token (15 минут)
   - Refresh token (7 дней)
   - Хранение в httpOnly cookies (для web) или в Keychain (для iOS)

5. **Миграция данных**
   - Создать скрипт для экспорта данных из CoreData
   - Создать endpoints для массовой загрузки
   - Трансформация данных из CoreData в PostgreSQL

### 3.2 Подготовка iOS приложения

**Приоритет: Высокий**  
**Срок: 2 недели**

#### Задачи:

1. **Создать слой абстракции данных**
   ```swift
   protocol DataRepository {
       func getGames(filter: GameFilter) async throws -> [Game]
       func createGame(_ game: Game) async throws -> Game
       func updateGame(_ game: Game) async throws
       func deleteGame(id: UUID) async throws
       // ... другие методы
   }
   
   class CoreDataRepository: DataRepository {
       // Текущая реализация
   }
   
   class APIRepository: DataRepository {
       // Новая реализация с API
   }
   ```

2. **Создать APIClient**
   ```swift
   class APIClient {
       let baseURL: String
       let session: URLSession
       
       func request<T: Decodable>(
           _ endpoint: Endpoint,
           method: HTTPMethod,
           body: Encodable?
       ) async throws -> T
   }
   ```

3. **Создать RepositoryFactory**
   ```swift
   class RepositoryFactory {
       static var shared = RepositoryFactory()
       
       var useAPI: Bool {
           UserDefaults.standard.bool(forKey: "useAPIBackend")
       }
       
       func makeRepository() -> DataRepository {
           if useAPI {
               return APIRepository()
           } else {
               return CoreDataRepository()
           }
       }
   }
   ```

4. **Обновить ViewModels**
   - Заменить прямые обращения к CoreData на использование Repository
   - Добавить обработку async/await
   - Добавить обработку ошибок сети

5. **Добавить настройки**
   - Раздел "Настройки" в приложении
   - Toggle "Использовать облачный бэкенд"
   - Поле для URL сервера
   - Кнопка "Синхронизировать данные"

6. **Реализация офлайн-режима**
   - Кэширование данных из API в CoreData
   - Очередь запросов для офлайн-операций
   - Синхронизация при восстановлении соединения

---

## Фаза 4: Система подписок

### 4.1 Интеграция StoreKit

**Приоритет: Средний**  
**Срок: 1-2 недели**

#### Задачи:

1. **Создать модели подписок**
   ```swift
   enum SubscriptionTier: String {
       case free
       case premium
       
       var features: [Feature] {
           // ...
       }
   }
   
   struct Feature {
       let name: String
       let description: String
       let isAvailable: Bool
   }
   ```

2. **Создать SubscriptionManager**
   ```swift
   @MainActor
   class SubscriptionManager: ObservableObject {
       @Published var currentSubscription: SubscriptionTier = .free
       @Published var products: [Product] = []
       
       func loadProducts() async
       func purchase(_ product: Product) async throws
       func restorePurchases() async throws
       func checkSubscriptionStatus() async
   }
   ```

3. **Создать PaywallView**
   - Красивый дизайн с премиум-фичами
   - Цены подписок
   - Кнопки "Попробовать бесплатно" / "Купить"
   - Кнопка "Восстановить покупки"

4. **Ограничения для бесплатной версии**
   - Лимит на количество игр (например, 10 игр)
   - Ограниченная статистика
   - Водяной знак на экспортируемых данных
   - Показывать Paywall при достижении лимита

5. **Backend интеграция**
   - Endpoint для валидации receipt в FastAPI
   - Синхронизация статуса подписки между устройствами
   - Webhook для уведомлений от App Store

---

## Фаза 5: Улучшение UI/UX

### 5.1 Редизайн главного экрана

**Приоритет: Высокий**  
**Срок: 2-3 недели**

#### Концепция дизайна: "Премиум покер-трекер"

**Цветовая палитра:**
- Основной: Темно-зеленый (#1A472A) - покерный стол
- Акцент: Золотой (#FFD700) - фишки, призы
- Фон: Глубокий черный (#0A0E0F) с градиентами
- Текст: Светло-серый (#E8E8E8), белый для важной информации
- Прибыль: Зеленый (#00C853)
- Убыток: Красный (#FF1744)

#### Задачи:

1. **Новый экран Dashboard**
   ```swift
   struct DashboardView: View {
       // Hero Section - Текущий баланс
       var balanceCard: some View {
           VStack {
               Text("Текущий баланс")
               Text("$\(balance)")
                   .font(.system(size: 48, weight: .bold))
                   .foregroundStyle(
                       .linearGradient(
                           colors: [.yellow, .orange],
                           startPoint: .leading,
                           endPoint: .trailing
                       )
                   )
               // Trend indicator
               HStack {
                   Image(systemName: trend > 0 ? "arrow.up.right" : "arrow.down.right")
                   Text("\(trend)% за месяц")
               }
           }
           .padding()
           .background(.ultraThinMaterial)
           .cornerRadius(20)
           .shadow(color: .black.opacity(0.3), radius: 10)
       }
       
       // Quick Stats Cards
       var statsGrid: some View {
           LazyVGrid(columns: [GridItem(), GridItem()]) {
               StatCard(title: "Игр создано", value: "\(gamesCreated)")
               StatCard(title: "Участвовал", value: "\(gamesParticipated)")
               StatCard(title: "Win Rate", value: "\(winRate)%")
               StatCard(title: "Лучшая сессия", value: "$\(bestSession)")
           }
       }
       
       // Recent Games List
       var recentGames: some View {
           // ...
       }
   }
   ```

2. **Анимации**
   - Анимация появления баланса с числовым счетчиком
   - Particle effect при добавлении крупного выигрыша
   - Spring анимации для карточек
   - Smooth transitions между экранами
   - Shimmer effect на золотых элементах
   - Конфетти при достижении milestone

3. **Компоненты**
   - **BalanceCardView**: Крупная карточка с балансом и трендом
   - **StatCardView**: Мини-карточки статистики с иконками
   - **GameRowView**: Строка игры с градиентом по результату
   - **PlayerAvatarView**: Аватар игрока с рейтингом
   - **ChartView**: Красивые графики с градиентами

4. **Графики и визуализация**
   - График баланса по времени (Swift Charts)
   - Круговая диаграмма по типам игр
   - Bar chart топ-игроков
   - Heat map активности по дням недели

### 5.2 Улучшение форм добавления

**Приоритет: Средний**  
**Срок: 1 неделя**

#### Задачи:

1. **AddGameSheet редизайн**
   - Пошаговый wizard вместо одной формы
   - Шаг 1: Выбор типа игры (большие кнопки с иконками)
   - Шаг 2: Добавление игроков (поиск + быстрое добавление)
   - Шаг 3: Buyins/Cashouts (числовая клавиатура, быстрые значения)
   - Шаг 4: Подтверждение (preview)
   - Progress indicator вверху

2. **Автозаполнение**
   - Предложения игроков на основе истории
   - Предложения buyins на основе средних значений
   - Быстрое копирование предыдущей игры

3. **Валидация**
   - Красивые inline ошибки
   - Подсказки при заполнении
   - Предупреждение при несбалансированных данных

### 5.3 Дополнительные фичи

**Приоритет: Низкий**  
**Срок: 1-2 недели**

#### Задачи:

1. **Achievements (Достижения)**
   - "Первая игра"
   - "10 игр подряд с прибылью"
   - "Покерная акула" (100 игр)
   - "Везунчик" (лучший единоразовый выигрыш)
   - Badge система
   - Анимация разблокировки

2. **Social Features**
   - Поделиться статистикой в соцсети (красивая карточка)
   - QR код профиля для быстрого добавления в игру
   - Leaderboard между друзьями (если они тоже используют приложение)

3. **Export/Import**
   - Экспорт в CSV/Excel
   - PDF отчет за период (с графиками)
   - Backup/Restore через iCloud

4. **Widgets**
   - Widget с текущим балансом
   - Widget со статистикой за неделю
   - Lock Screen widget (iOS 16+)

---

## Фаза 6: Тестирование и оптимизация

### 6.1 Unit тесты

**Приоритет: Высокий**  
**Срок: 1 неделя**

#### Задачи:
1. Тесты для всех Services
2. Тесты для ViewModels
3. Тесты для Repository слоя
4. Тесты миграции данных
5. Покрытие > 70%

### 6.2 UI тесты

**Приоритет: Средний**  
**Срок: 3-5 дней**

#### Задачи:
1. Тесты основных user flows
2. Тесты форм добавления игры
3. Тесты аутентификации
4. Snapshot тесты для UI компонентов

### 6.3 Оптимизация производительности

**Приоритет: Средний**  
**Срок: 3-5 дней**

#### Задачи:
1. Профилирование с Instruments
2. Оптимизация запросов CoreData
3. Lazy loading для больших списков
4. Кэширование изображений
5. Уменьшение размера bundle

---

## Фаза 7: Развертывание

### 7.1 Backend deployment

**Приоритет: Высокий**  
**Срок: 3-5 дней**

#### Варианты:
1. **AWS**
   - RDS для PostgreSQL
   - EC2 или ECS для FastAPI
   - CloudFront для CDN
   - Route53 для DNS

2. **DigitalOcean** (бюджетный вариант)
   - Managed PostgreSQL
   - App Platform для FastAPI
   - Spaces для файлов

3. **Railway** (самый простой)
   - Автоматический deploy из GitHub
   - Managed PostgreSQL
   - Бесплатный tier для начала

#### Задачи:
1. Настройка CI/CD (GitHub Actions)
2. Docker контейнеризация
3. Настройка мониторинга (Sentry, DataDog)
4. Настройка логирования
5. SSL сертификаты
6. Настройка backup для БД

### 7.2 iOS App Store

**Приоритет: Высокий**  
**Срок: 1 неделя**

#### Задачи:
1. Подготовка App Store Connect
2. Screenshots и preview видео
3. Описание приложения
4. Настройка IAP для подписок
5. Тестирование через TestFlight
6. Отправка на ревью

---

## Дополнительные улучшения

### Краткосрочные (1-2 месяца)

1. **Apple Watch приложение**
   - Быстрый просмотр баланса
   - Добавление результата игры
   - Уведомления о milestone

2. **iPad оптимизация**
   - Split view
   - Drag & drop
   - Расширенная статистика

3. **Темная/светлая тема**
   - Полная поддержка обеих тем
   - Автопереключение по времени суток

4. **Локализация**
   - Английский язык
   - Другие языки по запросу

### Среднесрочные (3-6 месяцев)

1. **AI Insights**
   - Предсказание результатов на основе истории
   - Рекомендации по банкролл менеджменту
   - Анализ паттернов игры

2. **Multi-currency поддержка**
   - Автоматическая конвертация
   - График баланса в разных валютах

3. **Tournament tracker**
   - Отслеживание турниров
   - Структура призов
   - ROI калькулятор

4. **Game notes**
   - Заметки к играм
   - Теги и категории
   - Поиск по заметкам

### Долгосрочные (6+ месяцев)

1. **Web версия**
   - React/Next.js фронтенд
   - Те же данные через FastAPI
   - Синхронизация с iOS

2. **Android версия**
   - Flutter или React Native
   - Полный feature parity

3. **Live game tracking**
   - Real-time обновления во время игры
   - Показ текущего стека каждого игрока
   - Таймер сессии

4. **Community features**
   - Публичные профили
   - Следить за друзьями
   - Сравнение статистики

---

## Timeline и приоритеты

### Sprint 1-2 (2-4 недели): MVP Backend + User System
- [ ] Создание FastAPI проекта
- [ ] Модели данных в PostgreSQL
- [ ] JWT аутентификация
- [ ] Базовые CRUD endpoints
- [ ] Расширение CoreData моделей
- [ ] AuthViewModel upgrade

### Sprint 3-4 (4-6 недель): Core Features
- [ ] Логика разделения игр
- [ ] PlayerClaimService
- [ ] Repository pattern в iOS
- [ ] APIClient
- [ ] Базовый редизайн Dashboard

### Sprint 5-6 (6-8 недель): UI/UX Enhancement
- [ ] Полный редизайн главного экрана
- [ ] Анимации
- [ ] Графики и визуализация
- [ ] Улучшение форм

### Sprint 7-8 (8-10 недель): Monetization
- [ ] StoreKit интеграция
- [ ] PaywallView
- [ ] Ограничения free tier
- [ ] Backend валидация подписок

### Sprint 9-10 (10-12 недель): Polish & Launch
- [ ] Тестирование
- [ ] Оптимизация
- [ ] Deployment backend
- [ ] TestFlight beta
- [ ] App Store submission

---

## Технический стек

### iOS
- **Framework**: SwiftUI
- **Минимальная версия**: iOS 16.0+
- **Persistence**: CoreData (временно) → API
- **Networking**: URLSession + async/await
- **Authentication**: Keychain + JWT
- **Payments**: StoreKit 2
- **Charts**: Swift Charts
- **Testing**: XCTest + ViewInspector

### Backend
- **Language**: Python 3.11+
- **Framework**: FastAPI
- **Database**: PostgreSQL 15+
- **ORM**: SQLAlchemy 2.0
- **Migrations**: Alembic
- **Authentication**: python-jose (JWT)
- **Password hashing**: passlib + bcrypt
- **Validation**: Pydantic v2
- **Testing**: pytest + httpx

### DevOps
- **Containerization**: Docker
- **CI/CD**: GitHub Actions
- **Hosting**: Railway / DigitalOcean / AWS
- **Monitoring**: Sentry
- **Logging**: structlog

---

## Оценка ресурсов

### Команда (идеально)
- 1 iOS разработчик (senior)
- 1 Backend разработчик (middle+)
- 1 UI/UX дизайнер (part-time)
- 1 QA engineer (part-time)

### Время
- **Минимальный MVP**: 2-3 месяца
- **Полная версия v1.0**: 4-6 месяцев
- **С дополнительными фичами**: 6-12 месяцев

### Бюджет (примерно)
- **Development**: зависит от команды
- **Apple Developer**: $99/год
- **Backend hosting**: $20-100/месяц (начало)
- **Database**: $15-50/месяц
- **Monitoring/Analytics**: $0-50/месяц
- **Domain**: $10-20/год

---

## Риски и митигация

### Технические риски
1. **Миграция данных может быть сложной**
   - Митигация: Тщательное тестирование, возможность rollback
   
2. **Производительность с большим количеством данных**
   - Митигация: Пагинация, индексы БД, кэширование

3. **Синхронизация между CoreData и API**
   - Митигация: Очередь операций, conflict resolution

### Бизнес риски
1. **Low adoption подписочной модели**
   - Митигация: Хороший free tier, ценное предложение в premium

2. **Конкуренция**
   - Митигация: Уникальные фичи (AI insights, social features)

---

## Метрики успеха

### Технические
- [ ] API response time < 200ms (p95)
- [ ] App launch time < 2s
- [ ] Crash rate < 1%
- [ ] Test coverage > 70%

### Продуктовые
- [ ] 1000+ установок за первый месяц
- [ ] 5% conversion в premium
- [ ] 4+ рейтинг в App Store
- [ ] 30%+ retention на 30 день

### Пользовательские
- [ ] Добавление игры < 1 минуты
- [ ] NPS > 40
- [ ] < 5% negative reviews

---

## Контакты и документация

### Репозитории (будущие)
- iOS App: `github.com/yourorg/poker-tracker-ios`
- Backend API: `github.com/yourorg/poker-tracker-api`

### Документация
- API docs: `api.yourapp.com/docs` (автогенерация FastAPI)
- iOS SDK docs: генерируется через DocC
- User guide: Notion или Confluence

---

## Заключение

Этот план представляет собой комплексную дорожную карту по превращению текущего PokerCardRecognizer в полноценное премиум-приложение для трекинга покерных игр с облачной синхронизацией, красивым UI и монетизацией.

Ключевые преимущества после реализации:
- ✅ Персонализация на основе пользователя
- ✅ Облачная синхронизация между устройствами
- ✅ Возможность масштабирования
- ✅ Монетизация через подписки
- ✅ Премиум UX/UI
- ✅ Расширяемая архитектура

**Рекомендуемый подход**: Начать с Фазы 1-3 для создания прочного фундамента, затем итеративно добавлять фичи из Фаз 4-7 на основе обратной связи пользователей.

Удачи в разработке! 🚀