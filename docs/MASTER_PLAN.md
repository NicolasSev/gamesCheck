# Мастер-план gamesCheck

> **Единый источник правды для агентов.** Всегда читай перед задачей.

**Обновление:** 2026-03-29 — **fishchips-web WEB_REDESIGN_PLAN Phase 12–13 (§7.2, §8):** админ `<lg` — `AdminLayoutShell` + Radix `Sheet` слева (`sheet.tsx`, `@radix-ui/react-dialog`), sticky header с hamburger; десктоп — фиксированный `Sidebar`, контент `lg:ml-60` / `ml-0`; responsive stat-grid (дашборд, analytics, import preview, player detail); класс `.transition-table-row` в `globals.css` (`var(--transition-fast)`) на `ui/table` `TableRow` и админских строках; grep `#hex` в `src/**/*.tsx` — нет совпадений. `npm run build` + Jest **100**. **Figma:** Prototype / Screen Map — при проходе: mobile admin (hamburger + off-canvas nav). — **GEMINI_PROMPT_FIX_PLAN:** Edge `recognize-cards` — системный промпт Gemini дополнен явным различием мастей по **форме** (♥/♦: скругления vs ромб, угол пипа в углу карты), `temperature` **0.2 → 0.1**, пользовательский хвост запроса: `SYSTEM_PROMPT + "\\n\\nAnalyze the image above."`; деплой Cloud: `npx supabase functions deploy recognize-cards --project-ref uffgwdmwufqududpyhbg`. Опциональный второй вызов (retry h/d) из плана **не** внедрён — по рекомендации плана «сначала промпт». Проверка: админка Card Vision, 5–10 фото с красными мастями. — **fishchips-web WEB_REDESIGN_PLAN Phase 11 (§4.7–4.14):** остаточные user-маршруты — `players` (list + `[id]`/`player-profile-client`), `profile` (`profile-form`, `web-push-card`), `claims`/`pending` (`pending-actions`), `notifications` (page + `notifications-client`), `games/new`, `join`, `import` (`user-import-form`, `import-conflicts-panel`); shared: `empty-state`, `photo-upload-zone`, `initials-avatar`, `pull-to-refresh`, `streak-card`, `top-record-card`, `game-calendar`, `player-game-card`, user `loading.tsx`, `overview-client`, `app/error` и `games/[id]/error`. Удалены алиасы `--user-*` из `globals.css`; классы на `--fg-*`, `--accent-*`, `--glass-*`, `--bg-*`; карточки форм — `glass` + поля `bg-[var(--bg-elevated)]/45`; primary — `gradient-green` + `text-[var(--bg-deepest)]`; непрочитанные уведомления — `ring-[var(--accent-green)]/35`. `npm run build` OK, Jest **100**. — **fishchips-web WEB_REDESIGN_PLAN Phase 10 (§4.3–4.5):** user `/app/games` — `GamesFilterBar` glass + активный `gradient-green`; пилюли дат/типов те же; поиск `glass`; строки игр `glass` + hover; прибыль `text-positive` / `text-negative`; FAB «+» `gradient-green` при `games.length > 0`; empty CTA + шапка «Новая» — `gradient-green`; `page.tsx` `pb-24`. `/app/games/[id]` — tooltip графика `bg-[var(--bg-elevated)]`, бары `var(--accent-green|red)`; карточки тип/баланс/бильярд — glass-bg + ring; primary-кнопки `gradient-green`; диалог удаления `bg-elevated`. `/hands` — glass-слоты/игроки; CTA фото `gradient-gold`; раскрытый блок `bg-[var(--bg-elevated)]/35`; `poker-card` / `card-picker` на токенах; `player-game-card` profit tokens. Убран `#0f0f23` из tooltip Recharts. `npm run build` OK. — **fishchips-web WEB_REDESIGN_PLAN Phase 9 (§4.2, §4.6):** игрок `/app` (обзор) и `/app/stats` — вместо `--user-*` семантика `--fg-primary` / `--fg-secondary` / `--fg-muted`, `--accent-green` / `--accent-gold`, `--glass-border` / `--glass-bg` / `--glass-hover`, CTA «Новая игра» `text-[var(--bg-deepest)]`; `BalanceHeroCard` — градиент суммы на токенах (плюс: green→sky→violet, минус: red→orange→red); `StatCard` — `GlassCard` + transition/hover на `--glass-hover`; `GamesAccordion` — секции месяца `glass`, разделители `--glass-border`; `user-stats-charts` — `CartesianGrid`/`Tooltip` как админ-аналитика (`--bg-elevated`, `--glass-border`, `--fg-primary`/`--fg-muted` на осях), линии/заливка `--accent-green`. `npm run build` OK. — **fishchips-web WEB_REDESIGN_PLAN Phase 8 (§4.1, §5, §6):** Player shell — header/tabbar `bg-[var(--bg-base)]/80`, `border-[var(--glass-border)]`, логотип градиент green→gold; `AppTabBar` — активный `--accent-green` + индикатор, неактивный `--fg-muted`, фон/bar на glass; `/login`, `/register` — glass card, поля `glass-bg` + focus green, кнопки `--accent-green`; `/admin/login` — золотой бейдж/градиент, glass card, поля focus gold, CTA `--accent-gold`. `npm run build` OK. **Figma:** Prototype — tab bar/header под те же токены. — **fishchips-web WEB_REDESIGN_PLAN Phase 7 (§3.9–3.12):** админ `/claims` — glass pills (all/pending/approved/rejected), бейдж счётчика на Pending, glass-таблица; Approve `gradient-green`, Reject red glass outline; `/analytics` — три `StatCard` glass, pills периода 7d/30d/90d/All + `games_by_day`, карточки/внутренние обёртки графиков glass; `analytics-charts.tsx` — `var(--chart-1..5)`, оси/tooltip на `--fg-muted` / `--bg-elevated`; `/push` — glass карточки, поля `glassField`, preview glass + иконка `gradient-green`, Send `gradient-green`, золотой акцент у Bell в шапке/строке устройств; `/card-vision` — upload dashed glass + gold hover/glow, результат glass, ошибка распознавания red border, Recognize green / Save `gradient-gold`, raw JSON glass. Jest: `sidebar.test` активный пункт — glass + green bar. `npm run build` + `npm test` (100) OK. — **Phase 5 (§3.4, §3.8):** админ `/users/[id]`, `/games/[id]` — glass header (аватар, username, слот Email «—» без новых fetch, User ID, Joined, Admin gold); сетка `MiniStat` glass; Recent games / aliases / claims — glass, `glass-hover` на строках; форма правки в glass; деталь игры — status line (host · date), balance glass + border `--accent-green`/`--accent-red`, Players/Batches в `glass rounded-xl`, шапки uppercase muted, счёт бильярда `--accent-gold`; `game-actions` Soft Delete — outline `--accent-red`. Данные/логика без изменений. `npm run build` OK. — **Phase 6 (§3.5–3.6):** админ `/players`, `/players/[playerKey]` — glass-поиск, pills Game/Profile (`gradient-green`) и Sort (gold active), список — `players-list-body.tsx` glass-карточки + `InitialsAvatar`, баланс/win/avg `text-positive`/`text-negative`, клик → деталь; деталь — hero glass, stat grid glass, Recent + Games history accordion glass, Linked profiles glass, chart/streaks glass. `npm run build` OK. **Figma:** при проходе — wireframe Players list/detail под glass cards. — **Phase 4 (§3.3, §3.7):** админ `/users`, `/games` — glass-поиск/фильтры, активный `gradient-green`, таблицы `glass rounded-xl`, шапка muted uppercase, hover `glass-hover`; Users — строка целиком кликабельна (`users-table-body.tsx`), колонка Email через `auth.admin.listUsers`, Admin-бейдж gold; Games — Import `gradient-gold`, тип — glass pill, игроки — малый badge. `npm run build` OK. — **Phase 3 (§3.2):** админ дашборд — `StatCard` glass + `transition-[all_var(--transition-normal)]`, hover `glass-hover` и `-translate-y-px`, `iconTone` + цветной бейдж (Tailwind bg-*/15), value/title/sub на `--fg-*` и `text-positive`/`text-negative`; секции графиков и Recent Games в glass; `GamesChart` — stroke/fill `var(--accent-green)` или `variant="gold"` (`--accent-gold`), grid/tooltip на glass/bg-elevated, `animationDuration={800}`; регистрации — второй чарт gold; таблица — uppercase muted headers, hover rows, soft_deleted `opacity-50`, тип игры — glass pill. Jest `stat-card` OK. `npm run build` OK. — **Phase 2 (§3.1):** админ `Sidebar` — `bg-[var(--bg-deepest)]`, glass border; логотип градиент green→gold, белый Spade, типографика Fish & Chips / Admin Panel; навигация — активный пункт `glass-hover` + зелёная полоса слева 3px, hover glass-bg, иконки 20px, `transition` через `var(--transition-fast)`; бейдж claims — красная pill; logout — `fg-muted`→primary. `npm run build` OK. — **Phase 1:** единые casino-токены в `globals.css` (`--bg-*`, `--fg-*`, `--accent-*`, glass/gradient utilities, shadcn map §0.4); `html.dark`, body `min-h-screen gradient-hero`; убран wrapper `.user-app`, анимация `app-main-enter`; временные алиасы `--user-*` в `:root` (Phase 11 — удалены, вызовы только на `--fg-*` / `--glass-*` / `--accent-*` / `--bg-*`); shell/login/register/loading на новых классах. `npm run build` OK. — **Баланс веб/iOS:** в данных `buyin` = **штуки** (не ₸), `cashout` и баланс = **₸**; баланс/профит = `cashout − buyin×2000`. UI: `formatBuyinUnits` / `formatTenge` в админке и user `PlayerGameCard` / деталь игры; аналитика — средний бай-ин по типу в **шт**, подпись с ≈₸. Миграция **`017_buyin_tenge_balance.sql`** + `buyin-tenge.ts` (ранее в этот день). — **Card Vision:** после биллинга Google вернул **404** на `gemini-2.0-flash` («модель недоступна новым пользователям»). В `fishchips-supabase/functions/recognize-cards/index.ts` дефолт **`GEMINI_MODEL` → `gemini-2.5-flash`**; деплой Cloud: **MCP `user-supabase` → `deploy_edge_function`** для `recognize-cards` (альтернатива: CLI `supabase functions deploy`). Повторная проверка вызовом: **HTTP 200**, `ok: true` (на тестовом 1×1 JPEG — пустой борд, `incomplete`). Если в Dashboard задан секрет `GEMINI_MODEL=gemini-2.0-flash` — убрать или заменить на `gemini-2.5-flash`. — Ранее: 429 free tier → биллинг; клиент парсит JSON при 502 (`recognize.ts`). Секреты `GEMINI_*` в Dashboard → Edge Functions. — 2026-03-28 — **Edge `recognize-cards`:** задеплоен в Cloud (`npx supabase@latest functions deploy … --project-ref uffgwdmwufqududpyhbg`); в `fishchips-supabase` добавлены `supabase/config.toml` + symlink `supabase/functions/recognize-cards` → `functions/recognize-cards` (CLI ищет `supabase/functions/`). — **Supabase Cloud:** применена миграция `fix_rls_recursion` (`016_fix_rls_recursion.sql`) — функция `check_game_access`, политики `game_players_select` / `games_select` без рекурсии RLS. — **UI_PARITY_PLAN — фазы 8–11 (закрыто в одной сессии):** покерные руки — `PokerCard` (`mini` / `md` / `lg` 44×64), `CardPicker` сетка **4×13** (масть × ранг), tap-to-pick; `hands/page` передаёт `creatorUsername`; `hands-client` — золотой CTA «Распознать с фото», борд с подписями Флоп/Тёрн/Ривер, форма на `GlassCard`, OddsResult в glass, список раздач — glass + сворачиваемый детальный борд/эквити, кнопка удаления вне `<summary>`; `/app/loading` — skeleton hero + сетка (pulse). Логин/регистрация (фаза 9): hero-анимация **scale 0.6→1** в `globals.css` (`user-auth-hero-in`). Фаза 10: `useCountUp` и `user-app-main-enter` уже были; hover/active на кнопках/строках; **10.3** — `SwipeDeleteRow`: свайп влево + «Удалить» на списке игр (только хост, `softDeleteGame`), на раздачах покера (хост) + кнопка «Удалить» на `sm+`; **10.4** — `PullToRefresh` в `UserAppShell` (pull-down при `scrollY≈0` → `router.refresh()`). Фаза 11: `AppTabBar` — подчёркивание активного таба (`after:bg-accent`). `npm run build`, Jest 100. **Figma:** при проходе — hands flow + tab bar. — **UI_PARITY_PLAN (продолжение ранее):** `/app/players`, профиль, claims, notifications (glass, badges). **Правило агента:** при запросе «выполни план целиком» — все фазы до критериев, без остановки на P0; см. `.cursor/rules/full-plan-execution.mdc`. — **Cursor:** правило `.cursor/rules/full-plan-execution.mdc` и скилл `.cursor/skills/execute-full-plan/SKILL.md` (выполнение markdown-планов целиком). — **UI_PARITY_PLAN (`fishchips-web` `/app`, ранее):** casino-токены, градиент `.user-app`, `.glass`, `GlassCard`, `useCountUp`; обзор — hero-баланс, 6 stat-карточек, горизонтальные недавние (`recentWithProfit`), chevron в аккордеоне; игры — `GamesFilterBar`, `fetchGamesRowMetaAction`, бейджи календаря, glass-разделители, empty state; статистика — 4 top-record, streak+`firstGameId`, max buy-in и «самая дорогая игра», progress по типам, клики по графикам; деталь игры — tooltip баров, `PlayerGameCard`, share-текст, превью раздач; shell — glass header/tabbar, badge на колоколе; login/register — градиент, иконки, Eye/confirm/email. Figma: при проходе — wireframes user app. — **ADMIN_PLAYERS_PLAN (веб-админка):** миграция `016_admin_player_stats_view.sql` — VIEW `admin_player_stats`, RPC `get_player_sessions` / `get_player_mvp_count` (применено в Supabase Cloud через MCP); страницы `/players`, `/players/[playerKey]`, запросы `src/lib/admin/queries/players.ts`, сайдбар Players, `isAdminPanelPath` + Jest; типы в `fishchips-web/src/types/supabase.ts`; для новых RPC второй аргумент `.rpc(..., … as never)` из‑за несовпадения вывода без `createServerClient<Database>`. `npm run build` + Jest **100**. **Figma Screen Map:** добавить админ-маршруты `/players` и деталь игрока + API-блок (VIEW/RPC). — **SUPABASE_DATA_FIX_PLAN (Cloud):** применена миграция **015** `user_statistics` без soft-deleted игр; очищены `poker_hands`, `billiard_batches`, `game_players`, `player_claims`, `games`; реимпорт 78 игр из `gamesCheck/TestData/ui_audit_import_games.txt` через `fishchips-web/scripts/reimport-ui-audit-data.mjs` (парсер как iOS); `game_players` для имени «Ник» привязаны к профилю `n1k`; верификация: 78 игр, 73 строк n1k в `user_statistics`. — **fishchips-web deploy (prod VPS):** `package.json` → `next build --webpack` — дефолтный Turbopack в Next 16 клал в `.next` внешние id вида `require-in-the-middle-<hash>`, на Linux после rsync с Mac модуль не резолвился → 500 на `/api/health`; webpack даёт обычный `require("require-in-the-middle")`. `deploy.sh`: на сервере `npm ci` (полный), без `next build` на VPS; проверка health в конце. — **REMAINING_WORK_PLAN:** Card Vision на Supabase Edge `recognize-cards` (Gemini; секреты `GEMINI_API_KEY` / `GEMINI_MODEL`); миграции `013` Storage `card-photos`, `014` поля `recognition_*` + RLS insert для super_admin; веб админ `/card-vision`, user `/app/games/[id]/hands` кнопка фото + `recognition_source: gemini`; iOS `GeminiCardRecognitionService` + «Фото (Gemini)» в `HandAddView` при онлайне; GitHub Actions CI (Jest + Playwright + опциональный deploy); n8n папка переименована в `fishchips-n8n-archived/`; middleware не трогает `/api/*`. — **STACK_IMPROVEMENTS_PLAN** (корень workspace): `fishchips-web` — `src/types/supabase.ts` + обёртка `database.ts`, `npm run db:types`, `TypedSupabase` без generic до автогена; Sentry (`instrumentation.ts`, `SentryProvider`, DSN в env); `/api/health`; `output: standalone`; `docker-compose.yml`, `npm run deploy:docker`, в `deploy.sh` проверка health после pm2; Jest: mock overview + `computeStatsTabData` + `game-actions`, `testPathIgnorePatterns` для helpers, `modulePathIgnorePatterns` для `.next`. **Supabase Cloud:** `010` (VIEW stats), `012` (`rate_limits` + `check_rate_limit`), **`011` (`poker_hands.players` JSONB, удалена `poker_hand_players`)**, Edge `send-push` с rate limit через RPC (деплой MCP). iOS руки покера — локально, без синка этой таблицы. **Фаза 8 STACK:** slice 1 — `SyncCoordinator` только Supabase+очередь; auth без CK restore; `BackendSwitch` default supabase. Дальше — удаление CK-сервисов/App (см. `STACK_IMPROVEMENTS_PLAN.md` §8). Чеклист и сводная таблица `STACK_IMPROVEMENTS_PLAN.md` обновлены (фазы 1–7 ✅). `npm run build` + **99** Jest OK. — 2026-03-27 — **IOS_TO_WEB_MIGRATION_PLAN** (корень workspace): реализованы фазы 1–10 в `fishchips-web` — обзор с `?player=`, MVP/лучшая сессия, аккордеон; список игр: календарь, «Загрузить ещё», группировка по дням; деталь: soft delete, тип/заметки, «Поделиться», гистограмма Recharts; статистика: MVP + rate, графики бай-инов и кумулятива; игроки: поиск, профиль с алиасами и кумулятивом; claims: заметки при решении, удаление своей pending, блок решённых; профиль: заявки, email read-only, удаление аккаунта (RPC); уведомления за 30 дней + старый realtime-клиент; join через server action; раздачи покера в Supabase (`006` + `011_poker_hands_jsonb`) + UI + Monte Carlo шансы (`fishchips-web/src/lib/poker-odds`, `calculatePokerOddsAsync` + прогресс, кнопка на `/app/games/[id]/hands`, эквити в JSONB `players[].win_percentage`); `007_player_claim_approved_links.sql` (триггер одобрения); триггер `008` удалён миграцией `011`; `loading.tsx` / `error.tsx`, `empty-state`, хук `useRealtimeRefresh` на ключевых экранах; `src/lib/user/queries.ts` реэкспортирует `./queries/index`. **Миграции 006–009 применены в Supabase Cloud (MCP).** **Web Push (веб):** миграция `009_web_push_subscriptions.sql`, карточка на `/app/profile`, `public/sw.js`, VAPID `NEXT_PUBLIC_VAPID_PUBLIC_KEY` + секреты Edge `send-push`; админ `/push` шлёт через `filter` all|user. `npm run build` + Jest 91/91 OK. **Figma:** обновить Screen Map под новые веб-фичи при проходе. — **Стадии плана (follow-up):** `fishchips-web` — Jest 87/87, `userEvent.setup({ delay: null })` в `push-form.test.tsx` (стабильность на полном прогоне); `npm run deploy` на VPS `185.146.3.87`, pm2 `fishchips-admin` online; HTTP 307 на `/` → `/admin/login`. **UI-audit:** `UI_AUDIT_SKIP_IMPORT=1` + `run_ui_audit.sh` (iPhone 16 sim) — XCUITest **TEST SUCCEEDED** (~133s), артефакты `artifacts/ui-audit/`. **Figma** — при необходимости обновить Screen Map вручную. — 2026-03-27 — **fishchips-n8n:** отдельный репозиторий `../fishchips-n8n/` — Docker (n8n + Postgres), workflow Card Vision: фото → **Gemini** (`GEMINI_API_KEY`) → `boardCards` / `players` (как в `HandModel`); локально `.env` с `POSTGRES_PASSWORD`, `N8N_ENCRYPTION_KEY`, `docker compose up`, импорт `workflows/card-vision.json`. VPS — фаза 2. — **P10–P14 (casino UI):** `NotificationsView` — `.scrollContentBackground(.hidden)`, `.casinoBackground()`, строки с `.liquidGlass` и белым текстом; `PendingClaimsView` — toolbar «Закрыть» (`dismiss`), фон через `.casinoBackground()` вместо дубля картинки; `ClaimDetailView` (тот же файл) — фон `.casinoBackground()`; `MyClaimsView` — `.casinoBackground()`, строка «Вы ещё не подали…»; `AddGameSheet` — `.scrollContentBackground(.hidden)`, `.preferredColorScheme(.dark)`, `.casinoBackground()`, белые кнопки toolbar. `ProfileView` без изменений. — **P15–P19 (casino UI):** `Color.casinoAccentGreen` / `casinoAccentGold` в `CasinoBackgroundModifier.swift`; `PlayerProfileView` / `PlayerProfileSelectionSheet` — зелёные акценты, sheet на `.casinoBackground()`; `HandAddView` / `HandEditView` — фон `.casinoBackground()`, CTA зелёный/золотой toolbar; `DebugView` — casino-фон, тёмный список; `ImportDataSheet` — тот же фон, зелёная кнопка валидации; `JoinGameByCodeSheet` — зелёные акценты + `.casinoBackground()` (a11y `join_game_code_field`, `join_game_sheet_cancel` без изменений); `SimpleTextEditor` — `textColor = UIColor.label` для тёмных форм. `MyClaimsView` / join-by-code идентификаторы для UI-audit не трогались. — **Auth/Splash (casino/Figma):** `SplashScreenView` — `.casinoBackground()`; `LoginView` / `RegistrationView` / `BiometricPromptView` — градиенты и primary-кнопки в зелёно-золотой палитре (Figma GREEN/GOLD), идентификаторы a11y без изменений. — **BilliardGameDetailView:** `.casinoBackground()` (единый модификатор с остальными экранами), a11y `billiard_detail_root`, `billiard_add_player_button`, `billiard_add_batch_button`. — **iOS GamesListTabView:** чипы фильтра игр — невыбранный текст `.white.opacity(0.7)` вместо `.primary` (консистентность с casino-темой и читаемость при светлой системной теме). — **fishchips-web iOS→веб паритет (карта + UI):** детальная секция и gap в [`WEB_USER_PLAN.md`](WEB_USER_PLAN.md); слой запросов `fishchips-web/src/lib/user/queries/` (`overview`, `games-list`, `stats`); `/app` — шесть карточек из `user_statistics` + недавние игры; `/app/games` — `fetchGamesListForUser` + клиент `GamesListClient` (фильтр покер/бильярд, поиск, режим «все даты» / «месяц»); `/app/stats` — `fetchStatsTabData` (по типам игры, крупнейший выигрыш/проигрыш, серии побед/поражений, график прибыли по сессиям); `/app/games/[id]` — блок «Прибыль по игрокам» (cashout−buyin). Маршруты и Supabase API без изменений; **Figma Screen Map** — при следующем проходе можно дополнить аннотации к веб-карточкам (тот же `/app/*`). `npm run build` OK. — **fishchips-web `/app/import`:** конфликты по датам как на iOS — `checkImportConflicts` + UI (пропуск/замена по дате, Replace all / Skip all), `importGamesAsUser` с `skippedDateKeys`, замена игры создателя за календарный день. В `parseImportText` добавлено поле `dateKey` (локальный YYYY-MM-DD). — **fishchips-web RU:** пользовательский UI (`/app`, `/login`, `/register`, `/admin/login`) — русские строки в `src/app/app/**`, `src/components/user/**`, auth-страницах. — **fishchips-web:** Jest — `src/lib/user/routes.test.ts` (маршруты user/admin/public, import `/app/import` vs `/games/import`), `src/__tests__/app-tab-bar.test.tsx` (4 вкладки по `href`). **Figma:** в Screen Map (файл `LOP4UJR4tMlmFWMhWA4dwM`) добавить веб-маршруты `/app/*` и `/admin/login` рядом с iOS-экранами (ручное обновление или MCP `plugin-figma-figma`). **fishchips-web user app:** префикс `/app` — паритет iOS (обзор, игры, статистика, игроки, профиль, claims, import, join, notifications, hands local). Админ: `/admin/login`, дашборд `/`. Миграция Supabase `005_user_rls_participants.sql` (участники видят приватные игры). `npm run build` / Jest OK. **Импорт:** сущность `Game` в Core Data больше не помечена как `abstract` (подклассов нет — вставка `Game(context:)` давала сбои save / validation). `DataImportService.importGames` выставляет `creator` по `userId`, убран промежуточный `save()` при «только обновить creatorUserId»; импорт из `ImportDataSheet` — одна транзакция и `replaceExistingForDate` для конфликтов. Ошибки save показывают `NSDetailedErrorsKey`, если есть. 2026-03-26 — **`MainViewModel`:** список игр и календарь на вкладке «Игры» берут данные из полного массива (`getGames` / фильтр), без `prefix(50)` — иначе при сортировке «новые сверху» старые годы (напр. все 23 сессии за 2024 из `ui_audit_import_games.txt`) не попадали в UI и в точки календаря, пока не вызовут `loadMore` много раз. 2026-03-27 — **`scripts/run_ui_audit.sh`** по умолчанию запускает `testUIAuditScreenFlowWithImport` (логин → импорт из `TestData/ui_audit_import_games.txt` → скрины/обход); без импорта: `UI_AUDIT_SKIP_IMPORT=1`. — **Импорт / UITest:** дамп игр в исходном виде — `gamesCheck/TestData/ui_audit_import_games.txt`; `DataImportService.parseText` понимает те же строки (в т.ч. пробелы у скобок, `~`, `+/-`, суммы через `+`, незакрытая `)`, дата `DD.MM`, разделители U+2028); UITest: аргумент `--uitesting-import-file` и env `UITEST_IMPORT_FILE_PATH` подставляют текст в `ImportDataSheet`; `testUIAuditScreenFlowWithImport` — валидация, при конфликтах дат сначала прокрутка `import_conflicts_scroll` к низу, затем тап «Импортировать» (`import_conflicts_confirm_replace`, замена всех непропущенных дат), выбор «Ник», затем обход как в `testUIAuditScreenFlow`. `UITestCredentials.resolveRepoRoot()` ищет корень по `FishAndChips.xcodeproj`, не только по `.env.ui-audit`. — **UI-audit (логин в XCUITest):** `FC_TEST_*` дублируются в `app.launchEnvironment` (схема даёт env только раннеру); `LoginView` при `--uitesting` подставляет email/пароль из env приложения; `--uitesting-auto-submit-login`: `LoginView` после prefill сам вызывает `login()` (обход тапа); `LoginPage` сначала ждёт `screen.main` / «Вход...», затем резервно тап/typeText; `MainPage`: вкладки по `identifier` в `descendants` + fallback `tabBars.buttons[label]`; Debug в UI-audit: свайпы до hittable, иначе шаг пропускается. Join sheet: `join_game_sheet_cancel` + `dismissJoinGameByCodeSheet` (без swipe); `screenflow.json`/PNG — `ScreenshotCapture` пишет в `gamesCheck/artifacts/ui-audit` без env (раньше уходило в Temp → пустой манифест в репо). поля ищутся по `identifier` в `descendants`, не только в `textFields` (SwiftUI). Если схема Xcode не раскрыла `$(FC_TEST_*)`, в env попадает буквальная строка — `UITestEnvValidation` + `run_ui_audit.sh` отсекают; `LoginView` не подставляет такие значения. Ранее: без тапа «Нет аккаунта?» перед входом; без анимации `isAppearing` в UITest. — **UI-audit (ранее):** `FishAndChipsUITests/UIAuditUITests` + `artifacts/ui-audit/` (`screenflow.json`, `screenshots/` в .gitignore), запуск `scripts/run_ui_audit.sh` (нужны `FC_TEST_EMAIL`/`FC_TEST_PASSWORD`, `UI_AUDIT_ARTIFACTS_ROOT`); хелперы в `FishAndChipsUITests/Support/`; `screen.login` / `screen.main` для a11y; схема: `-parallel-testing-enabled NO` для UITests. **Figma:** в файле Interactive Prototype добавлена страница «iOS ScreenFlow Audit» (MCP `use_figma`). — Auth: пароль только ASCII + `.keyboardType(.asciiCapable)`; ошибка `passwordNotLatin`; убран вывод пароля в лог; снова «Пользователь не найден» после неуспешного Supabase+CloudKit (без отдельного алерта по `invalid_credentials` от API). Figma Prototype: подмена wireframe на скриншоты с устройства — в репо `scripts/capture_simulator_screenshot.sh` (снимок Booted-симулятора в PNG); загрузка PNG в слои Figma вручную или через плагин (MCP не передаёт бинарные скрины с симулятора). | [DATA_DIAGRAM](DATA_DIAGRAM.md) | [QUICK_REF](QUICK_REF.md)

---

## ⛔ Правила (ОБЯЗАТЕЛЬНО)

1. **Читай MASTER_PLAN.md** в начале каждого запроса
2. **При изменении данных** — обновляй `docs/DATA_DIAGRAM.md` (entity, атрибуты, schema, flow)
3. **При завершении задач** — обновляй этот файл (статус, дата)
4. **Не создавай новые .md** без явного разрешения. Обновляй существующие.
5. **Supabase:** Small diffs → компиляция → проверка в Dashboard → следующий шаг

---

## Архитектура

**МИГРАЦИЯ: CloudKit → Supabase (в процессе)**

**Архитектура sync (2026-03-28): Supabase + Core Data + OfflineSyncQueue**  
**Supabase = Source of Truth.** Core Data = локальный кэш. Офлайн — запись в очередь, при сети — только Supabase.  
**SyncCoordinator** — центральный координатор. Online → Supabase; offline → Core Data + `OfflineSyncQueue` (без CloudKit в координаторе).  
**NetworkMonitor** — отслеживает состояние сети, при reconnect автоматически обрабатывает очередь.

**Supabase таблицы:** profiles (User+PlayerProfile), games, game_players, player_aliases, player_claims, device_tokens  
**Views (PostgreSQL):** game_summaries, user_statistics (обычные VIEW; см. миграция 010), admin_player_stats (016 — агрегаты по имени игрока для админки)  
**RLS:** Row Level Security на всех таблицах  
**Realtime:** WebSocket подписки на games, claims, profiles

**CloudKit (legacy, до удаления):**  
Public DB: Game, GameWithPlayer, PlayerAlias, PlayerProfile, PlayerClaim, User

---

## Текущий статус

**Синхронизация Supabase-first ✅** (обновлено 2026-03-28):
- ✅ Phase 1: NetworkMonitor (NWPathMonitor) + SyncServiceProtocol
- ✅ Phase 2: SyncCoordinator — online Supabase / offline очередь (slice 1: CloudKit убран из координатора)
- ✅ Phase 3: Rewire 15+ Views/Services на SyncCoordinator
- ✅ Phase 4: AuthViewModel — Supabase при сети; офлайн только локальный пользователь
- ✅ Phase 5: OfflineSyncQueue — auto-reconnect, exponential backoff
- ✅ Phase 6: Conflict resolution (SQL 004 + server-wins strategy)
- ✅ Phase 7: Web-admin safety (83/83 tests pass, build OK)
- ✅ Phase 8: iOS tests pass (pre-existing failures only)
- ✅ Phase 9: DebugView Backend Status + docs update

**Реструктуризация монорепо → 3 репозитория ✅** (2026-03-18):
- ✅ iOS остался в `gamesCheck/` (текущий git)
- ✅ Web Admin → `../fishchips-web/` (новый git, `main` ветка)
- ✅ Supabase → `../fishchips-supabase/` (новый git, `main` ветка)
- ✅ `.gitignore` в iOS репозитории обновлён

**Миграция CloudKit → Supabase ✅** (2026-03-18):
- ✅ Phase 0: SPM supabase-swift 2.41.1, SupabaseConfig, BackendServiceProtocol
- ✅ Phase 1: SQL schema (001_initial_schema.sql) — таблицы, RLS, triggers, materialized views
- ✅ Phase 2: DTO (SupabaseDTO.swift, SupabaseModelConverters.swift)
- ✅ Phase 3: SupabaseAuthService (Supabase Auth, bcrypt)
- ✅ Phase 4: SupabaseService (CRUD обёртка, retry, ошибки)
- ✅ Phase 5: SupabaseSyncService (push/pull, smart sync, merge, pending)
- ✅ Phase 6: SupabaseRealtimeService (WebSocket подписки)
- ✅ Phase 7: SyncRouter + BackendSwitch (переключение бэкендов)
- ✅ Phase 8: Тесты (MockSupabaseService/Auth, DTO, Errors, BackendSwitch)
- ✅ Phase 9: DataMigrationToSupabase (клиентская миграция)
- ✅ Phase 10: DATA_DIAGRAM v3.0, Cursor rules
- ✅ Phase 11: Improvements (GDPR deletion, OfflineSyncQueue, Edge Function push, server validation)

**UI/UX улучшения ✅** (2026-03-19):
- ✅ Casino-тема на LoginView, RegistrationView, BiometricPromptView, PlayersTabView
- ✅ Анимация переходов между auth-состояниями (ContentView)
- ✅ Исправлен календарь в GamesListTabView (offset дней недели)
- ✅ Единообразный фон .casinoBackground() на всех экранах (убран inline background)
- ✅ ProfileView: скрыты технические данные, подтверждение выхода, аватар
- ✅ Усилен LiquidGlass эффект (gradient border, deeper shadow)
- ✅ Haptic feedback на ключевые действия (buyin +/-, создание игры, sync, login, register)
- ✅ Убраны устаревшие ссылки на CloudKit из UI (SplashScreen, MainView overlay)

**Supabase URL (важно):** правильный ref — `uffgwdmwufqududpyhbg`. Опечатки ведут на чужой проект / NXDOMAIN → `invalid_credentials`: `uffgwd**sw**…`, `uffgw**fmw**…`, `uffgwd**mr**uf**qw**…`, `uffgwd**xw**…` (xw↔mw). В `SupabaseConfig.swift` автозамена известных ref; в `Info.plist` — канонический URL без опечаток.

**Supabase Cloud + VPS Deploy ✅** (2026-03-26):
- ✅ Supabase Cloud проект `uffgwdmwufqududpyhbg` (PostgreSQL 17)
- ✅ 4 миграции применены через MCP (execute_sql): 7 таблиц, 12 функций, 5 views, RLS, Realtime
- ✅ MCP Cursor подключён (`user-supabase`, OAuth)
- ✅ VPS PS.kz: Ubuntu 24.04, 185.146.3.87, Node 20, nginx, pm2
- ✅ fishchips-web задеплоен: `http://185.146.3.87` → nginx → pm2 (fishchips-admin)
- ✅ UFW: SSH + Nginx Full
- ✅ pm2 autostart (systemd)
- ✅ Cursor rules: `vps-deploy.mdc`, `supabase-cloud.mdc`

**iOS Screen Map → Figma + Web Plan ✅** (2026-03-26):
- ✅ FigJam навигационная диаграмма: 27 экранов iOS, все переходы
- ✅ Figma Design файл: карточки экранов с API/Data аннотациями, Supabase schema
- ✅ **Interactive Prototype: 20 экранов, casino-тема, кликабельные переходы, API-аннотации**
- ✅ Web User Plan: 10 фаз, приоритеты P0-P3, endpoints, структура маршрутов
- 📄 `docs/WEB_USER_PLAN.md` — план для агентов web-версии
- 🎨 Figma: [Prototype](https://www.figma.com/design/LpguHhVtpfW8xo7Qz0TCc3) | [Screen Map](https://www.figma.com/design/LOP4UJR4tMlmFWMhWA4dwM) | [Flow Diagram](https://www.figma.com/online-whiteboard/create-diagram/622ce98e-e547-4682-9304-8b3f106b3a67)

**Предыдущие:** Push fix ✅ | Рефакторинг ✅ | Фаза 3 ✅ | Фаза 2 ✅

---

## Правила при работе с Supabase

- После изменения схемы → SQL migration файл в **`../fishchips-supabase/migrations/`** → применить через MCP `execute_sql`
- Проверять RLS-политики после изменения таблиц
- Small diffs: один сервис/метод за раз, не батчить
- BackendSwitch.isSupabase — проверять перед sync вызовами

---

## Структура проекта

### iOS (текущий репозиторий — `gamesCheck/`)
- **Persistence**: `Persistence.swift` (core) + `Persistence+*.swift` (5 extension-файлов)
- **Supabase**: `Services/Supabase/` — SupabaseConfig, SupabaseService, SupabaseAuthService, SupabaseSyncService, SupabaseRealtimeService, SyncCoordinator, BackendSwitch+SyncRouter (legacy), DataMigrationToSupabase, OfflineSyncQueue, AccountDeletionService
- **Network**: `Services/NetworkMonitor.swift` — NWPathMonitor wrapper, auto-reconnect trigger
- **Supabase Models**: `Models/Supabase/` — SupabaseDTO, SupabaseModelConverters
- **CloudKit (legacy)**: `CloudKitService.swift`, `CloudKitSyncService.swift`, `CloudKitModels.swift`
- **Логирование**: `debugLog()` из `DebugLogger.swift` — все логи только в DEBUG
- **UI-компоненты**: `CasinoBackgroundModifier`, `LiquidGlassModifier`, `CurrencyFormatting`, `PokerUIHelpers`, `PokerCardViews`
- **Тесты**: Swift Testing (unit) + XCTest (UI), моки в `FishAndChipsTests/Mocks/`
- **CI/CD**: `.github/workflows/ci.yml`

### Web Admin (отдельный репозиторий — `../fishchips-web/`)
- Next.js 16 + Tailwind CSS + shadcn/ui
- Страницы (admin): Dashboard, Users, Games, Claims, Analytics, Push Notifications — `/`, `/admin/login`
- Страницы (user-facing): **`/app/*`** — см. `docs/WEB_USER_PLAN.md` (Auth `/login`, Register, Overview, Games, Stats, Players, Profile, Claims, Import, Join, Notifications, Hands local)
- Тесты: Jest + React Testing Library
- **Прод:** `http://185.146.3.87` — VPS PS.kz, Ubuntu 24.04, Node 20, nginx → pm2 (`fishchips-admin`), `/home/ubuntu/fishchips-web/`
- Деплой: локальный `npm run build`, `rsync` **с `.next`**, на VPS только `npm ci --omit=dev` + `pm2 restart` (без `next build` на сервере — см. `vps-deploy.mdc`)
- HTTPS — после привязки домена (certbot), обновить URL в Supabase Auth

### Supabase (отдельный репозиторий — `../fishchips-supabase/`)
- **Облако:** проект `uffgwdmwufqududpyhbg`, PostgreSQL 17, MCP `user-supabase`
- **SQL миграции**: `migrations/` — 001–004 (таблицы, RLS, triggers, views, conflict resolution)
- **Edge Functions**: `functions/send-push/` — APNs push-уведомления

### Card Vision (актуально)
- **Supabase Edge Function** `recognize-cards` — основной путь (фото → Gemini → JSON карт). Секреты в Dashboard → Edge Functions.
- **Архив n8n:** `fishchips-n8n-archived/` (legacy Docker workflow); не использовать для новых фич.

---

## Ссылки

- Supabase SDK (iOS): `SupabaseService.swift`, `SupabaseSyncService.swift`, `SupabaseAuthService.swift`
- CloudKit (legacy): `CloudKitService.swift`, `CloudKitSyncService.swift`
- [DATA_DIAGRAM.md](DATA_DIAGRAM.md) | [WEB_USER_PLAN.md](WEB_USER_PLAN.md) | [Supabase Dashboard](https://supabase.com/dashboard/project/uffgwdmwufqududpyhbg)
- **Figma:** [Interactive Prototype](https://www.figma.com/design/LpguHhVtpfW8xo7Qz0TCc3) | [Screen Map](https://www.figma.com/design/LOP4UJR4tMlmFWMhWA4dwM) | [Flow Diagram](https://www.figma.com/online-whiteboard/create-diagram/622ce98e-e547-4682-9304-8b3f106b3a67)
- **fishchips-web**: `../fishchips-web/` — веб-админка | **Прод:** `http://185.146.3.87`
- **fishchips-supabase**: `../fishchips-supabase/` — SQL миграции и Edge Functions
- **fishchips-n8n**: `../fishchips-n8n/` — n8n Card Vision (Docker), webhook распознавания карт
- **Cursor rules:** `vps-deploy.mdc` (сервер), `supabase-cloud.mdc` (Supabase схема/MCP)

---

## Чеклист для агента

- [ ] Прочитал MASTER_PLAN.md
- [ ] Не создаю MD без разрешения
- [ ] Обновляю DATA_DIAGRAM при изменении данных
- [ ] Small diffs + проверка в Supabase Dashboard
- [ ] Проверяю BackendSwitch при изменении sync-логики
